<%= simple_form_for( @problem, html: { class: 'form-group' }) do |f| %>
  <%= f.error_notification %>
  <%= f.input :title, label: 'Title' %>
  <%= f.button :submit, class: "btn-primary" %>
<% end %>
